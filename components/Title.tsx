import styled from 'styled-components'

const Title = styled.h1`
  font-size: 24px;
  font-weight: 600;
  line-height: 29px;
  color: var(--color-black);
  margin: 0;
`

export default Title
